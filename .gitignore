# Build output
build/
bin/
lib/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code 本地配置(per-repo agent collaboration tooling)
.claude/
# 协作文档保留在 DeviceOps 伞仓维护;此处 untracked 不入仓
AGENTS.md
CLAUDE.md
# GitNexus 本地 ignore 配置
.gitnexusignore

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Android build
android/.gradle/
android/CMakeCache.txt
android/CMakeFiles/
android/app/.cxx/
android/app/build/
android/app/proguard-rules.pro

# Desktop build (separate working dir)
build-desktop/

# Proto generated files (temporary build dir)
# Proto source files (keep in git):
#   gen-android/  → Android proto (v4.25.1)
#   gen-desktop/ → Desktop proto (v34.1)
gen/

# gRPC prebuilt libraries (committed to git, persistent)
# android/grpc-android-prebuilt/<ABI>/
build-verify/
tools/protoc-*
.gitnexus
.grpc-build/

# stray build/work dirs
build-linux/
build-android-arm64-s3/
build-android-armeabi-s3/
build-review-s2-arm64/
build-review-b5fix-v7a/
.understand-anything/
