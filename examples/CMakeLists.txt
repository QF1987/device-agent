# Examples for device-agent
#
# Build with:
#   cd examples
#   mkdir build && cd build
#   cmake .. && make

cmake_minimum_required(VERSION 3.16)

add_subdirectory(pharmacy_app)
add_subdirectory(rd_vnc_standalone)
