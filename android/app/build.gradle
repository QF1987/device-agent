plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.deviceagent'
    compileSdk 34
    ndkVersion '30.0.14904198'

    defaultConfig {
        applicationId "com.deviceagent"
        minSdk 24
        targetSdk 34
        versionCode 83
        versionName "2.2.0-fix48"

        ndk {
            abiFilters 'arm64-v8a', 'armeabi-v7a'
        }

        buildConfigField "String", "DEFAULT_SERVER_URL", "\"http://192.168.31.81:8080\""
        buildConfigField "String", "DEFAULT_DEVICE_ID", "\"ANDROID-001\""
        buildConfigField "String", "NORMAL_INSTALL_METHOD", "\"session_commit\""

        externalNativeBuild {
            cmake {
                arguments "-DANDROID_STL=c++_shared"
            }
        }
    }

    buildTypes {
        debug {
            debuggable true
        }
        release {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = '17'
    }

    // CMake 配置
    externalNativeBuild {
        cmake {
            path "../../CMakeLists.txt"
            version "3.22.1"
        }
    }

    // 启用 Prefab（从 AAR 中导出 native 库给 CMake 使用）
    // 需要 Android Gradle Plugin 8.2+
    buildFeatures {
        buildConfig = true
        prefab true
    }

    // 安装方式 flavor
    flavorDimensions += "installer"
    productFlavors {
        silent {
            dimension "installer"
            buildConfigField "String", "INSTALL_MODE", "\"silent\""
        }
        custom {
            dimension "installer"
            buildConfigField "String", "INSTALL_MODE", "\"custom\""
        }
        normal {
            dimension "installer"
            buildConfigField "String", "INSTALL_MODE", "\"normal\""
            // 普通安装实现方式：action_view（默认,MIUI兼容）| session_commit（标准API）
            buildConfigField "String", "NORMAL_INSTALL_METHOD", "\"action_view\""
        }
    }
}

dependencies {
    // gRPC Android (包含 Java API + native .so)
    implementation 'io.grpc:grpc-android:1.62.2'
    implementation 'io.grpc:grpc-okhttp:1.62.2'  // gRPC 使用 OkHttp
    implementation 'com.google.protobuf:protobuf-javalite:3.25.3'

    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'org.json:json:20231013'
    implementation 'androidx.core:core-ktx:1.12.0'
    implementation 'androidx.lifecycle:lifecycle-service:2.7.0'
    implementation 'org.jetbrains.kotlin:kotlin-stdlib:1.9.22'
}
